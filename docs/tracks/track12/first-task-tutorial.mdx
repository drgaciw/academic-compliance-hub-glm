# First Task Tutorial

This tutorial walks you through completing your first task in the Athletic Academics Hub codebase.

## Prerequisites

Before starting, ensure you've completed the [Local Development Setup](./local-setup).

## Task: Add a "Contact Support" Button

We'll add a "Contact Support" button to the main application landing page that opens a modal form.

### Task Breakdown

1. Create the modal component
2. Create the form component
3. Add the button to the landing page
4. Wire up the API endpoint
5. Test the feature

## Step 1: Create the Modal Component

First, let's create a reusable modal component.

### Create File

Create `packages/ui/src/components/contact-modal.tsx`:

```typescript
'use client';

import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from '@packages/ui/components/dialog';
import { Button } from '@packages/ui/components/button';
import { useState } from 'react';

export function ContactModal() {
  const [open, setOpen] = useState(false);

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button variant="outline">Contact Support</Button>
      </DialogTrigger>
      <DialogContent className="sm:max-w-[425px]">
        <DialogHeader>
          <DialogTitle>Contact Support</DialogTitle>
          <DialogDescription>
            Send us a message and we'll get back to you within 24 hours.
          </DialogDescription>
        </DialogHeader>
        <ContactForm onSuccess={() => setOpen(false)} />
      </DialogContent>
    </Dialog>
  );
}
```

## Step 2: Create the Form Component

Create `packages/ui/src/components/contact-form.tsx`:

```typescript
'use client';

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@packages/ui/components/form';
import { Input } from '@packages/ui/components/input';
import { Textarea } from '@packages/ui/components/textarea';
import { Button } from '@packages/ui/components/button';

const contactSchema = z.object({
  name: z.string().min(2, 'Name must be at least 2 characters'),
  email: z.string().email('Invalid email address'),
  subject: z.string().min(5, 'Subject must be at least 5 characters'),
  message: z.string().min(10, 'Message must be at least 10 characters'),
});

type ContactFormData = z.infer<typeof contactSchema>;

interface ContactFormProps {
  onSuccess: () => void;
}

export function ContactForm({ onSuccess }: ContactFormProps) {
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [submitStatus, setSubmitStatus] = useState<'idle' | 'success' | 'error'>('idle');

  const form = useForm<ContactFormData>({
    resolver: zodResolver(contactSchema),
    defaultValues: {
      name: '',
      email: '',
      subject: '',
      message: '',
    },
  });

  const onSubmit = async (data: ContactFormData) => {
    setIsSubmitting(true);
    try {
      const response = await fetch('/api/support/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      if (response.ok) {
        setSubmitStatus('success');
        setTimeout(() => {
          onSuccess();
          setSubmitStatus('idle');
        }, 2000);
      } else {
        setSubmitStatus('error');
      }
    } catch (error) {
      setSubmitStatus('error');
    } finally {
      setIsSubmitting(false);
    }
  };

  if (submitStatus === 'success') {
    return (
      <div className="text-center py-8">
        <div className="text-green-600 mb-2">
          <svg className="w-12 h-12 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
          </svg>
        </div>
        <h3 className="text-lg font-semibold mb-2">Message Sent!</h3>
        <p className="text-gray-600">We'll get back to you within 24 hours.</p>
      </div>
    );
  }

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
        <FormField
          control={form.control}
          name="name"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Name</FormLabel>
              <FormControl>
                <Input placeholder="Your name" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="email"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Email</FormLabel>
              <FormControl>
                <Input type="email" placeholder="you@example.com" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="subject"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Subject</FormLabel>
              <FormControl>
                <Input placeholder="Brief subject" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="message"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Message</FormLabel>
              <FormControl>
                <Textarea
                  placeholder="Describe your issue or question..."
                  className="min-h-[120px]"
                  {...field}
                />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />

        {submitStatus === 'error' && (
          <p className="text-sm text-red-600">
            Failed to send message. Please try again.
          </p>
        )}

        <div className="flex justify-end gap-2">
          <Button type="button" variant="outline" onClick={() => form.reset()}>
            Clear
          </Button>
          <Button type="submit" disabled={isSubmitting}>
            {isSubmitting ? 'Sending...' : 'Send Message'}
          </Button>
        </div>
      </form>
    </Form>
  );
}
```

## Step 3: Export from UI Package

Update `packages/ui/src/index.ts`:

```typescript
export * from "./components/contact-modal";
export * from "./components/contact-form";
```

## Step 4: Add Button to Landing Page

Update `apps/main/app/(marketing)/page.tsx`:

```typescript
import { ContactModal } from '@packages/ui';

export default function LandingPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      <nav className="flex justify-between items-center p-6 max-w-7xl mx-auto">
        <h1 className="text-2xl font-bold text-gray-900">
          Athletic Academics Hub
        </h1>
        <div className="flex gap-4">
          <ContactModal />
          <Button variant="default">Sign In</Button>
        </div>
      </nav>

      {/* Rest of your landing page content */}
    </div>
  );
}
```

## Step 5: Create API Endpoint

Create `apps/main/app/api/support/contact/route.ts`:

```typescript
import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";

const contactSchema = z.object({
  name: z.string().min(2),
  email: z.string().email(),
  subject: z.string().min(5),
  message: z.string().min(10),
});

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();

    // Validate input
    const validatedData = contactSchema.parse(body);

    // TODO: Send email or store in database
    // For now, just log the data
    console.log("Support request received:", validatedData);

    // Simulate processing time
    await new Promise((resolve) => setTimeout(resolve, 500));

    return NextResponse.json(
      { message: "Message sent successfully" },
      { status: 200 },
    );
  } catch (error) {
    if (error instanceof z.ZodError) {
      return NextResponse.json(
        { message: "Invalid input", errors: error.errors },
        { status: 400 },
      );
    }

    console.error("Error processing contact form:", error);
    return NextResponse.json(
      { message: "Internal server error" },
      { status: 500 },
    );
  }
}
```

## Step 6: Test the Feature

### 1. Start the Development Server

```bash
pnpm dev:main
```

### 2. Navigate to the Application

Open http://localhost:3000 in your browser.

### 3. Test the Modal

1. Click the "Contact Support" button
2. The modal should open
3. Try submitting the form with:
   - Invalid data (should see validation errors)
   - Valid data (should see success message)
4. Verify the modal closes after success

### 4. Check Console Logs

Open browser DevTools and check for console logs from the API endpoint.

## Step 7: Add Tests

Create `apps/main/__tests__/api/support/contact.test.ts`:

```typescript
import { POST } from "../app/api/support/contact/route";

describe("Contact API", () => {
  it("should validate input", async () => {
    const request = new Request("http://localhost:3000/api/support/contact", {
      method: "POST",
      body: JSON.stringify({
        name: "A", // Too short
        email: "invalid-email",
        subject: "Hi", // Too short
        message: "Short", // Too short
      }),
    });

    const response = await POST(request as any);
    const data = await response.json();

    expect(response.status).toBe(400);
    expect(data.message).toBe("Invalid input");
    expect(data.errors).toBeDefined();
  });

  it("should accept valid input", async () => {
    const request = new Request("http://localhost:3000/api/support/contact", {
      method: "POST",
      body: JSON.stringify({
        name: "John Doe",
        email: "john@example.com",
        subject: "Help needed",
        message: "I need help with the platform",
      }),
    });

    const response = await POST(request as any);
    const data = await response.json();

    expect(response.status).toBe(200);
    expect(data.message).toBe("Message sent successfully");
  });
});
```

## Step 8: Run Linting and Type Checking

```bash
# Lint
pnpm lint

# Type check
pnpm type-check

# Run tests
pnpm test
```

## Step 9: Commit Your Changes

```bash
git add .
git commit -m "feat: add contact support modal and form"
```

## What You've Learned

In this tutorial, you:

1. **Created reusable UI components** in the shared package
2. **Used form validation** with react-hook-form and Zod
3. **Implemented API endpoints** with Next.js API routes
4. **Handled form state** with loading, success, and error states
5. **Tested the feature** both manually and with tests
6. **Followed project conventions** for code organization

## Next Steps

Now that you've completed your first task, try these additional challenges:

1. **Add email integration** - Use a service like Resend or SendGrid to actually send emails
2. **Add file upload** - Allow users to attach screenshots or documents
3. **Create Storybook stories** - Document the modal and form components
4. **Add analytics** - Track support form submissions
5. **Implement ticket system** - Store support requests in the database

## Getting Help

If you run into issues:

- Check the [Troubleshooting FAQ](./troubleshooting-faq)
- Review the [Architecture Overview](./architecture-overview)
- Ask questions in the team Discord

<Callout type="success">
  Congratulations! You've successfully completed your first task. Continue
  exploring the codebase and contributing to the project.
</Callout>
